# 🗺️ Улучшения для карт и компонентов (v5.1)

## ✅ Что было улучшено

### 1. **Обработка масок**
- Добавлена поддержка свойства `isMask`
- Логирование элементов-масок для диагностики
- Правильная обработка изображений с масками

### 2. **Улучшенная обработка INSTANCE (компонентов)**
- Компоненты помечаются при конвертации в FRAME
- Сохраняется информация о `clipsContent` для правильной обрезки
- Логирование оригинального `componentId` для отладки

### 3. **Расширенная диагностика изображений**
- Детальное логирование всех параметров изображений
- Проверка наличия URL и base64 данных
- Предупреждения о проблемных изображениях

### 4. **Улучшенная обработка дочерних элементов**
- Проверка масок среди детей
- Расширенная поддержка ELLIPSE для масок
- Диагностика отсутствующих данных изображений

## 📊 Что теперь логируется

### Для масок:
```
[MASK] Applied isMask=true to node: MaskElement
[CHILDREN] Child MapMask is a MASK
```

### Для компонентов:
```
[INSTANCE] Converting INSTANCE to FRAME for node: Flag
[INSTANCE] Processing component properties for: Flag
[INSTANCE] Applied clipsContent=true
[INSTANCE] Original componentId: 123:456
```

### Для изображений:
```
[PAINTS] Processing image fill: {
  hasImageData: false,
  isBig: true,
  hasImageUrl: true,
  imageRef: "abc123",
  scaleMode: "FILL"
}
[CHILDREN] Image fill 0 in MapImage has no URL or data!
```

## 🔍 Как диагностировать проблему с картой

1. **Запустите плагин версии 5.1**
2. **Откройте консоль Figma** (Ctrl+Alt+I)
3. **Попробуйте скопировать проблемный слайд**
4. **В консоли ищите:**
   - Сообщения о масках (`MASK`)
   - Предупреждения об изображениях без данных
   - Информацию о компонентах (`INSTANCE`)

## ⚠️ Известные ограничения

### Карты могут не копироваться если:
1. **Слишком большое изображение** - API Figma имеет лимиты на размер
2. **Сложная структура масок** - вложенные маски могут не поддерживаться
3. **Приватные изображения** - требуют специальных прав доступа

### Флаги могут быть квадратными если:
1. **Потеря обрезки** - clipsContent не применяется правильно
2. **Компонент с масками** - сложная структура компонента
3. **Векторные маски** - не все типы масок поддерживаются

## 🚀 Следующие шаги

Если проблема сохраняется:
1. Поделитесь логами из консоли
2. Укажите структуру проблемных элементов
3. Возможно, потребуется дополнительная обработка специфичных случаев

## 💡 Временное решение

Пока проблема не решена полностью, можно:
1. Экспортировать карту как отдельное изображение
2. Использовать более простую структуру для флагов
3. Избегать сложных масок и вложенных компонентов 