# План тестирования исправлений SlideDeck 2.0

## 🚀 Подготовка к тестированию

1. **Проверка запуска сервера:**
   - Откройте браузер: http://localhost:3000
   - Убедитесь что страница загружается
   - Проверьте консоль браузера на ошибки (F12)

2. **Авторизация:**
   - Войдите в систему (если требуется)
   - Убедитесь что userId доступен в localStorage или сессии

---

## 📋 Чеклист тестирования исправлений

### ✅ 1. Preview Quality Change (Изменение качества превью)

**Шаги:**
1. Перейдите на страницу Moderation (`/moderation`)
2. Найдите слайд с превью
3. Откройте меню слайда (три точки)
4. Выберите "Quality" → выберите другое качество (например, 1x → 2x)
5. Дождитесь обновления превью

**Ожидаемый результат:**
- ✅ Превью обновляется с новым качеством
- ✅ Изображение загружается с новым URL (проверьте Network tab)
- ✅ В консоли нет ошибок

**Проверка в DevTools:**
- Network tab: запрос к `/api/figma/image-proxy` с правильным `scale`
- Network tab: PATCH запрос к `/api/slides/[id]` с обновленным `imageUrl`
- Console: нет ошибок при обновлении

---

### ✅ 2. Добавление в избранное

**Шаги:**
1. На главной странице (`/`) найдите слайд
2. Наведите на слайд и нажмите кнопку "Избранное" (звездочка)
3. Проверьте что иконка изменилась (заполненная звездочка)
4. Обновите страницу - статус должен сохраниться
5. Нажмите еще раз - слайд должен удалиться из избранного

**Ожидаемый результат:**
- ✅ Кнопка избранного работает
- ✅ Статус сохраняется после перезагрузки
- ✅ Можно добавлять и удалять из избранного

**Проверка в DevTools:**
- Network tab: POST запрос к `/api/slides/[id]/favorite` при добавлении
- Network tab: DELETE запрос к `/api/slides/[id]/favorite` при удалении
- Network tab: GET запрос к `/api/slides` содержит `userId` в query params
- Response: слайды содержат поле `isFavorite: true/false`

---

### ✅ 3. Поиск в Header (верхняя строка поиска)

**Шаги:**
1. На главной странице найдите кнопку поиска в Header (верхняя панель)
2. Нажмите на кнопку поиска
3. Проверьте что фокус установлен на поисковую строку в основном контенте
4. Введите текст для поиска
5. Проверьте что результаты фильтруются

**Ожидаемый результат:**
- ✅ При клике на кнопку поиска в Header фокус устанавливается на поисковую строку
- ✅ Можно сразу вводить текст
- ✅ Поиск работает корректно

**Проверка в DevTools:**
- Console: нет ошибок при клике
- Elements: input получает фокус (проверьте `document.activeElement`)

---

### ✅ 4. Отображение категорий (не "Uncategorized")

**Шаги:**
1. На главной странице найдите слайды с категориями
2. Проверьте отображение категорий:
   - В карточке слайда (внизу слева, желтый бейдж)
   - В деталях слайда (иконка папки)
3. Убедитесь что категории отображаются правильно

**Ожидаемый результат:**
- ✅ Слайды с категориями показывают правильные названия категорий
- ✅ Не показывается "Uncategorized" для слайдов с категориями
- ✅ Категории отображаются в карточке и в деталях

**Проверка в DevTools:**
- Network tab: GET `/api/slides` возвращает `SlideCategory` с вложенным `Category`
- Elements: проверьте что в DOM отображаются правильные названия категорий

---

### ✅ 5. Дубликаты - просмотр деталей

**Шаги:**
1. Перейдите на страницу Moderation (`/moderation`)
2. Перейдите на вкладку "Duplicates"
3. Найдите дубликат слайда
4. Нажмите кнопку "View Details"
5. Проверьте что открылось модальное окно с деталями слайда

**Ожидаемый результат:**
- ✅ Кнопка "View Details" работает
- ✅ Открывается модальное окно с полными деталями слайда
- ✅ Все поля слайда отображаются корректно
- ✅ Можно редактировать слайд

**Проверка в DevTools:**
- Network tab: GET запрос к `/api/slides/[id]` выполняется перед открытием модального окна
- Console: нет ошибок при загрузке данных

---

### ✅ 6. Quick Filters (быстрые фильтры)

**Шаги:**
1. На главной странице найдите Quick Filters (кнопки над поиском)
2. Нажмите "Favorites" (если есть избранные слайды)
3. Проверьте что отображаются только избранные слайды
4. Нажмите "Recent 7d" - проверьте фильтрацию по дате
5. Нажмите "Recent 30d" - проверьте фильтрацию
6. Нажмите "All" - проверьте что показываются все слайды

**Ожидаемый результат:**
- ✅ Quick Filters работают корректно
- ✅ Фильтрация по избранному работает
- ✅ Фильтрация по дате работает
- ✅ Можно вернуться к "All"

**Проверка в DevTools:**
- Network tab: GET `/api/slides` содержит правильные query параметры:
  - `favorite=true` для избранного
  - `days=7` или `days=30` для фильтрации по дате
- Response: возвращаются только отфильтрованные слайды

---

### ✅ 7. Filters - исправление NaN

**Шаги:**
1. На главной странице откройте панель фильтров (Metadata Filters)
2. Выберите несколько фильтров (теги, продукты, компоненты и т.д.)
3. Проверьте счетчик активных фильтров (желтый бейдж)
4. Убедитесь что показывается число, а не "NaN"

**Ожидаемый результат:**
- ✅ Счетчик фильтров показывает правильное число
- ✅ Не показывается "NaN"
- ✅ При добавлении/удалении фильтров счетчик обновляется

**Проверка в DevTools:**
- Elements: проверьте что в span с классом `bg-yellow-500` отображается число
- Console: нет ошибок при подсчете фильтров

---

### ✅ 8. Load More - сохранение состояния при переключении категорий

**Шаги:**
1. На главной странице прокрутите вниз и нажмите "Load More" несколько раз
2. Загрузите 2-3 страницы слайдов
3. Переключитесь на категорию слева (например, выберите другую категорию)
4. Вернитесь обратно к предыдущей категории (или "All")
5. Проверьте что ранее загруженные слайды сохранились

**Ожидаемый результат:**
- ✅ При переключении категорий слайды не теряются
- ✅ При возврате к категории показываются ранее загруженные слайды
- ✅ "Load More" продолжает работать корректно

**Проверка в DevTools:**
- Network tab: при возврате к категории не загружаются все слайды заново
- Console: проверьте что используется кэш категорий

---

### ✅ 9. Сохранение изменений в новых слайдах (drafts)

**Шаги:**
1. Перейдите на страницу Moderation (`/moderation`)
2. Найдите слайд со статусом "Draft" (не approved)
3. Откройте слайд для редактирования (View Details)
4. Измените несколько полей (название, описание, теги, категории и т.д.)
5. Нажмите "Save"
6. Закройте модальное окно и откройте снова
7. Проверьте что изменения сохранились

**Ожидаемый результат:**
- ✅ Изменения сохраняются корректно
- ✅ Все поля обновляются (включая массивы категорий, тегов и т.д.)
- ✅ После перезагрузки страницы изменения сохраняются

**Проверка в DevTools:**
- Network tab: PATCH запрос к `/api/slides/[id]` содержит все измененные поля
- Response: возвращается обновленный слайд
- Console: нет ошибок при сохранении

---

### ✅ 10. Изменения не теряются после Approve

**Шаги:**
1. Перейдите на страницу Moderation (`/moderation`)
2. Найдите слайд со статусом "Draft"
3. Откройте слайд и внесите изменения (например, измените название, добавьте теги)
4. Сохраните изменения (Save)
5. Закройте модальное окно
6. Нажмите "Approve" или "Publish" для этого слайда
7. Откройте слайд снова (он должен быть в списке approved)
8. Проверьте что все изменения сохранились

**Ожидаемый результат:**
- ✅ После approve все изменения сохраняются
- ✅ Статус меняется на "approved"
- ✅ `isActive` устанавливается в `true`
- ✅ Все метаданные и поля остаются без изменений

**Проверка в DevTools:**
- Network tab: PATCH запрос к `/api/slides/[id]` содержит и `isActive: true`, и все остальные поля
- Response: слайд обновлен со всеми полями
- Console: нет ошибок

---

## 🐛 Дополнительные проверки

### Проверка API endpoints:

1. **GET /api/slides**
   - Проверьте что возвращается правильная структура данных
   - Проверьте что `isFavorite` присутствует в ответе (если передан userId)
   - Проверьте фильтрацию по `favorite` и `days`

2. **POST /api/slides/[id]/favorite**
   - Проверьте что создается запись в таблице `FavoriteSlide`
   - Проверьте что возвращается успешный ответ

3. **DELETE /api/slides/[id]/favorite**
   - Проверьте что удаляется запись из таблицы `FavoriteSlide`
   - Проверьте что возвращается успешный ответ

4. **PATCH /api/slides/[id]**
   - Проверьте что обновляются все поля включая `isActive`
   - Проверьте что обрабатываются массивы (categoryIds, productCodes и т.д.)

---

## 📝 Чеклист для финальной проверки

- [ ] Все 10 проблем исправлены
- [ ] Нет ошибок в консоли браузера
- [ ] Нет ошибок в терминале (где запущен dev server)
- [ ] Все API запросы возвращают успешные ответы
- [ ] Данные сохраняются в базу данных корректно
- [ ] UI обновляется правильно после всех действий

---

## 🔍 Полезные команды для отладки

```bash
# Проверка подключения к БД
npx prisma studio

# Просмотр логов сервера
# (в терминале где запущен npm run dev)

# Проверка структуры данных в БД
npx prisma db pull
```

---

## 📌 Важные заметки

1. **База данных:** Убедитесь что подключены к dev базе (`slidebox_dev`), а не к prod
2. **Авторизация:** Для тестирования избранного нужен авторизованный пользователь
3. **Figma API:** Для тестирования preview quality нужен валидный Figma токен (обновляется через Settings)
4. **Кэш:** При тестировании Load More проверьте что кэш работает корректно
5. **Figma Token:** Токен теперь берется из базы данных через `getFigmaAccessToken()`. При обновлении токена через Settings кэш автоматически обновляется.

---

**Удачного тестирования! 🚀**

