# Исправление иерархии категорий

## Проблемы и решения

### 1. ✅ Убран текст из description при импорте

**Проблема:** При импорте автоматически добавлялся извлеченный текст в description.

**Решение:** В `app/api/slides/import/route.ts` изменено:
```typescript
// Было:
description: extractedText.substring(0, 200) + (extractedText.length > 200 ? '...' : ''),

// Стало:
description: null, // Убираем автоматическое добавление текста в description
```

### 2. ✅ Исправлена логика отображения категорий

**Проблема:** Родительские категории не отображались в библиотеке, даже если в их подкатегориях были слайды.

**Решение:** В `components/GroupedSlideGrid.tsx` обновлена логика:

#### Показ родительских категорий:
- Родительская категория показывается если в ней ИЛИ её подкатегориях есть слайды
- Счетчик родительской категории = прямые слайды + слайды всех подкатегорий

#### Визуальное отличие:
- **Родительские категории:** градиентный фон, метка "Category", без стрелки раскрытия
- **Подкатегории:** отступ слева, синяя полоска, можно раскрывать/закрывать
- **Uncategorized:** обычное отображение

### 3. ✅ Запрет добавления слайдов в родительские категории

**Проблема:** Можно было добавлять слайды в родительские категории, что нарушало логику иерархии.

**Решения:**

#### В интерфейсе импорта:
- Убраны родительские категории из селекта
- Доступны только подкатегории для выбора

#### В библиотеке:
- Родительские категории служат только заголовками
- Не показывают прямые слайды (даже если они есть)
- Не раскрываются (нет стрелки)

## Новая логика категорий

### Структура:
```
🏢 Company Overview (0 slides) ← Родительская категория
  ├── 📋 Mission, Vision, Values (1 slide) ← Подкатегория
  └── 📊 Company Experience (2 slides) ← Подкатегория

🛠️ Cross-Functional Expertise (3 slides) ← Родительская (сумма подкатегорий)
  ├── 🎨 Design (UX/UI) (1 slide) ← Подкатегория
  └── 🔧 Support and IT Operations (2 slides) ← Подкатегория
```

### Правила:
1. **Родительские категории** - только заголовки, показывают общее количество
2. **Подкатегории** - содержат реальные слайды
3. **При импорте** - можно выбирать только подкатегории
4. **В поиске** - текст сохраняется в `extractedText`, но не в `description`

## Результат

✅ **Четкая иерархия:** Родительские → Подкатегории  
✅ **Правильные счетчики:** Суммируются слайды подкатегорий  
✅ **Ограниченный выбор:** Нельзя добавлять в родительские категории  
✅ **Чистый интерфейс:** Нет автотекста в описаниях  
✅ **Улучшенный поиск:** Качественный извлеченный текст  

Теперь библиотека имеет логичную структуру с четким разделением между организационными категориями и категориями с содержимым! 