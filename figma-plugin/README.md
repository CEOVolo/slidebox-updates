# SlideDeck 2.0 - Figma плагин

Плагин для создания презентаций в Figma из библиотеки слайдов SlideDeck.

## 🚀 Функции

- ✅ Создание новых страниц в Figma для презентаций
- ✅ Копирование слайдов из других файлов Figma
- ✅ Автоматическая организация слайдов на странице
- ✅ Интеграция с веб-приложением SlideDeck
- ✅ Поддержка batch-копирования множественных слайдов
- ✅ Отчёты о результатах создания презентации

## 📦 Установка

### 1. Сборка плагина

```bash
cd figma-plugin
npm install
npm run build
```

### 2. Установка в Figma

1. Откройте Figma Desktop или Figma в браузере
2. Перейдите в меню `Plugins` → `Development` → `Import plugin from manifest...`
3. Выберите файл `figma-plugin/dist/manifest.json`
4. Плагин будет добавлен в раздел "Development" в меню плагинов

### 3. Альтернативный способ установки

1. Скопируйте содержимое папки `figma-plugin/dist/` в любую папку
2. В Figma: `Plugins` → `Development` → `Import plugin from manifest...`
3. Выберите файл `manifest.json` из скопированной папки

## 🎯 Использование

### Создание презентации через веб-приложение

1. Откройте веб-приложение SlideDeck 2.0
2. Создайте презентацию в конструкторе
3. Выберите "Figma плагин" как метод создания
4. Нажмите "Создать в Figma"
5. Веб-приложение автоматически активирует плагин в Figma

### Использование плагина напрямую

1. Откройте файл Figma, где хотите создать презентацию
2. Запустите плагин: `Plugins` → `Development` → `SlideDeck 2.0 - Presentation Creator`
3. Введите данные презентации вручную или используйте данные от веб-приложения
4. Нажмите "Создать презентацию в Figma"

## ⚙️ Технические особенности

### Поддерживаемые операции

- **Клонирование слайдов** - Копирование слайдов из того же файла Figma
- **Импорт слайдов** - Создание плейсхолдеров для слайдов из других файлов
- **Организация слайдов** - Автоматическое расположение в сетке 3 колонки
- **Управление страницами** - Создание новых страниц для презентаций

### Ограничения Figma API

⚠️ **Важно**: Figma Plugin API имеет ограничения:

1. **Прямой импорт из других файлов** - невозможен через публичное API
2. **Требуется доступ к исходным файлам** - файлы должны быть открыты или доступны
3. **Копирование узлов между файлами** - ограничено Plugin API

### Обходные решения

Для слайдов из внешних файлов плагин:
- Создаёт плейсхолдеры с информацией об исходном слайде
- Сохраняет метаданные (файл, узел, название)
- Предоставляет визуальную структуру презентации

## 🔧 Разработка

### Структура проекта

```
figma-plugin/
├── code.ts          # Основной код плагина
├── ui.html          # Интерфейс плагина
├── manifest.json    # Манифест плагина
├── types.d.ts       # TypeScript типы
├── package.json     # Зависимости
├── tsconfig.json    # Конфигурация TypeScript
└── dist/            # Собранные файлы
```

### Команды разработки

```bash
# Сборка плагина
npm run build

# Слежение за изменениями TypeScript
npm run watch

# Разработка (сборка + слежение)
npm run dev
```

### Отладка

1. Откройте Developer Tools в Figma (Ctrl+Option+I / Ctrl+Shift+I)
2. Во вкладке Console будут отображаться логи плагина
3. Используйте `console.log()` в коде для отладки

## 🌐 Интеграция с веб-приложением

### API Endpoints

Плагин интегрируется с веб-приложением через следующие endpoints:

- `GET /api/presentations/plugin?presentationId=ID` - Получение данных презентации
- `POST /api/presentations/plugin` - Отправка результатов создания

### Формат данных

```typescript
interface PresentationRequest {
  title: string;
  description?: string;
  slides: SlideData[];
  webAppUrl: string;
}

interface SlideData {
  id: string;
  title: string;
  figmaFileId: string;
  figmaNodeId: string;
  imageUrl?: string;
  order: number;
}
```

## 📋 Требования

- **Figma Desktop** v116+ или **Figma в браузере**
- **Node.js** v16+ (для разработки)
- **TypeScript** v5+ (для разработки)
- **Доступ к исходным файлам Figma** с слайдами

## 🔐 Разрешения

Плагин запрашивает следующие разрешения:
- `currentuser` - Доступ к информации о текущем пользователе
- `networkAccess` - Доступ к сети для связи с веб-приложением

## 🆘 Поддержка

### Часто встречающиеся проблемы

**Q: Плагин не может найти слайды из других файлов**
A: Убедитесь, что исходные файлы открыты в Figma или доступны в вашей команде

**Q: Ошибка "Cannot find node"**
A: Проверьте, что узел не был удалён или перемещён в исходном файле

**Q: Плагин не активируется из веб-приложения**
A: Проверьте, что плагин установлен и Figma открыт

### Логи и отладка

Все важные операции логируются в Developer Console Figma:
- Получение сообщений от UI
- Процесс копирования слайдов
- Ошибки и исключения
- Результаты операций

## 📝 Changelog

### v2.0.0 (Текущая версия)
- ✅ Базовая функциональность создания презентаций
- ✅ Интеграция с веб-приложением SlideDeck
- ✅ Поддержка клонирования слайдов в том же файле
- ✅ Плейсхолдеры для внешних слайдов
- ✅ Автоматическая организация слайдов

### Планируемые улучшения
- 🔄 Расширенная поддержка импорта между файлами
- 🔄 Пользовательские шаблоны раскладки
- 🔄 Экспорт презентаций в PDF
- 🔄 Интеграция с библиотеками компонентов

## 📄 Лицензия

MIT License - смотрите файл LICENSE для деталей. 