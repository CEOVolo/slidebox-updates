# 🚀 Универсальная система управления данными

> Гибкая, масштабируемая система для работы с любыми типами данных в SlideDeck 2.0

## 🎯 Что это?

Это эволюция нашей системы миграции метаданных. Теперь вы можете легко работать с **любыми** типами данных, а не только с метаданными.

## ⚡ Быстрый старт

```bash
# Миграция метаданных dev → prod
npm run data:migrate-dev-to-prod

# Засев метаданных
npm run data:seed-metadata

# Полная синхронизация
npm run data:full-sync
```

## 🔧 Основные возможности

### ✨ Универсальность
- **6 категорий данных**: от статических справочников до пользовательского контента
- **Гибкая конфигурация**: легко добавлять новые таблицы и категории
- **Автоматическое управление зависимостями**

### 🚀 Производительность
- **Батчевая обработка** для больших таблиц
- **Параллельное выполнение** задач
- **Умное кэширование** и оптимизация запросов

### 🔒 Безопасность
- **Автоматическое исключение** чувствительных данных
- **Трансформации данных** для обеспечения целостности
- **Валидация зависимостей** перед выполнением

### 🎛️ Гибкость
- **Тестовый режим** (`--dry-run`)
- **Выборочная миграция** (категории/таблицы)
- **Пропуск существующих** данных
- **Настраиваемые размеры** батчей

## 📋 Категории данных

| Категория | Описание | Примеры |
|-----------|----------|---------|
| `static_metadata` | Статические справочники | статусы, форматы, языки |
| `core_metadata` | Основные метаданные | категории, теги, домены |
| `business_metadata` | Бизнес справочники | продукты, компоненты |
| `content_data` | Контентные данные | слайды, документы |
| `user_data` | Пользовательские данные | пользователи, профили |
| `relationship_data` | Связи между сущностями | many-to-many отношения |

## 🛠️ Использование

### Миграция данных

```bash
# Все данные
npm run migrate:universal

# Конкретные категории
npm run migrate:universal -- --categories static_metadata,core_metadata

# Конкретные таблицы
npm run migrate:universal -- --tables category,product,tag

# С очисткой целевой БД
npm run migrate:universal -- --clear

# Тестовый запуск
npm run migrate:universal -- --dry-run
```

### Засев данных

```bash
# Управляемый засев
npm run seed:managed

# В конкретную среду
npm run seed:managed -- --env prod

# Параллельно
npm run seed:managed -- --parallel

# Пропустить существующие
npm run seed:managed -- --skip-existing
```

## 📝 Добавление новых данных

### 1. Конфигурация (`config/migration-config.ts`)

```typescript
{
  tableName: 'newTable',
  displayName: 'Новая таблица',
  icon: '📝',
  dependencies: ['existingTable'],
  specialHandling: 'batch',
  batchSize: 100
}
```

### 2. Скрипт засева (`scripts/seed-new-table.ts`)

```typescript
export async function seedNewTable() {
  // логика засева
}
```

### 3. Интеграция

Обновить mapping в `data-seeding-manager.ts`

## 📚 Документация

| Файл | Описание |
|------|----------|
| `UNIVERSAL_DATA_MANAGEMENT_GUIDE.md` | 📖 Полное руководство |
| `UNIVERSAL_DATA_CHECKLIST.md` | ✅ Чеклист для разработчиков |
| `PRODUCTION_DEPLOYMENT_GUIDE.md` | 🚀 Руководство по деплою |

## 🎉 Преимущества

- **🔧 Простота**: Готовые команды для типовых сценариев
- **⚡ Скорость**: Параллельная и батчевая обработка
- **🛡️ Надежность**: Валидация, откат, безопасность
- **📈 Масштабируемость**: Легко добавлять новые типы данных
- **🎯 Гибкость**: Множество опций для разных сценариев

## 🔄 Миграция с старой системы

Старые команды продолжают работать:
```bash
npm run seed:all        # работает как раньше
npm run migrate:metadata # работает как раньше
```

Новые команды - это дополнительные возможности!

## 🆘 Помощь

```bash
# Справка по миграции
npm run migrate:universal -- --help

# Справка по засеву
npm run seed:managed -- --help
```

---

**🎯 Результат**: Универсальная, гибкая система управления данными, которая растет вместе с вашим проектом! 