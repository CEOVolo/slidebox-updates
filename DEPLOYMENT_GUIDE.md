# 🚀 Полное руководство по деплою на Production

## 📋 Что нужно задеплоить:

### 1. Новый функционал (код):
- ✅ Улучшенное отображение всех метаданных в модальном окне слайда
- ✅ Кликабельные теги с автоматической фильтрацией
- ✅ Компонент MetadataFilter для фильтрации по всем метаданным
- ✅ Оптимизированная загрузка изображений (lazy loading + кэширование)
- ✅ Поддержка URL параметров для сохранения состояния фильтров
- ✅ Английская локализация интерфейса

### 2. Метаданные (данные БД):
- ✅ Categories с иерархией
- ✅ Tags, Products, Components, etc.

## 🔄 Порядок деплоя:

### Шаг 1: Деплой кода на production

```bash
# 1. Commit всех изменений
git add .
git commit -m "feat: Enhanced metadata display, filtering, and performance optimizations"

# 2. Push в main ветку
git push origin main

# GitHub Actions автоматически задеплоит код на production
```

### Шаг 2: Проверка деплоя кода

```bash
# Откройте production сайт
http://135.181.148.104:3000

# Проверьте:
# - Загружается ли сайт
# - Работают ли новые компоненты
# - Нет ли ошибок в консоли
```

### Шаг 3: Миграция метаданных

После успешного деплоя кода, мигрируйте метаданные:

```bash
# Вариант 1: Локально
export DATABASE_URL_DEV="postgresql://slidebox_dev_user:andersen_win@135.181.148.104:5432/slidebox_dev?schema=public"
export DATABASE_URL_PROD="postgresql://slidebox_ad:andersen_win@135.181.148.104:5432/slidebox_prod?schema=public"
npm run migrate:metadata

# Вариант 2: Через GitHub Actions
# Actions → Migrate Metadata to Production → Run workflow
```

### Шаг 4: Финальная проверка

1. **Проверьте новый функционал:**
   - Откройте модальное окно слайда - должны отображаться все метаданные
   - Кликните на тег - должна произойти фильтрация
   - Откройте фильтры - должны быть все категории метаданных
   - Проверьте скорость загрузки превью

2. **Проверьте метаданные:**
   ```bash
   DATABASE_URL="postgresql://slidebox_ad:andersen_win@135.181.148.104:5432/slidebox_prod?schema=public" npm run verify:metadata
   ```

## ⚠️ Важные замечания:

1. **Деплой кода ДОЛЖЕН быть первым** - новый код умеет работать со старыми данными
2. **Миграция метаданных вторая** - после деплоя кода
3. **Не забудьте проверить** работоспособность после каждого шага

## 🔍 Что проверить после деплоя:

### Функционал:
- [ ] Модальное окно показывает все метаданные с правильными именами
- [ ] Теги кликабельные и фильтруют слайды
- [ ] Компонент фильтров работает для всех типов метаданных
- [ ] Изображения загружаются быстрее (lazy loading)
- [ ] URL обновляется при применении фильтров
- [ ] Интерфейс на английском языке

### Данные:
- [ ] Все категории на месте с правильной иерархией
- [ ] Теги перенесены
- [ ] Все типы метаданных доступны в фильтрах

## 🚨 Откат в случае проблем:

### Откат кода:
```bash
# На сервере
cd /home/slidedeck/app
git log --oneline -5  # Найдите предыдущий коммит
git reset --hard <previous-commit-hash>
docker compose -f docker-compose.prod.yml down
docker compose -f docker-compose.prod.yml up -d --build
```

### Откат метаданных:
Если у вас есть бэкап (через export:metadata), можно восстановить.
Иначе придется мигрировать обратно с production на dev.

## 📊 Мониторинг после деплоя:

1. **Следите за логами:**
   ```bash
   docker compose -f docker-compose.prod.yml logs -f app
   ```

2. **Проверьте производительность:**
   - Скорость загрузки страниц
   - Скорость загрузки изображений
   - Работа фильтров

3. **Собирайте обратную связь:**
   - Работают ли все функции
   - Есть ли проблемы с отображением

## ✅ Чеклист для деплоя:

- [ ] Все изменения закоммичены
- [ ] Тесты пройдены локально
- [ ] Создан бэкап метаданных (npm run export:metadata)
- [ ] Push в main ветку выполнен
- [ ] GitHub Actions успешно выполнился
- [ ] Production сайт доступен
- [ ] Метаданные мигрированы
- [ ] Все функции проверены
- [ ] Логи не содержат ошибок

---

## 🎉 После успешного деплоя:

1. Документируйте изменения
2. Оповестите команду
3. Мониторьте первые часы работы
4. Будьте готовы к быстрому откату если нужно 